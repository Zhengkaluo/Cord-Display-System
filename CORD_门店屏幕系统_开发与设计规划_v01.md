# CORD 门店屏幕系统｜开发与设计规划 v0.1

更新日期：2026-09-01  
目标屏幕：Samsung The Frame 32LS03C，1920 × 1080，横屏  
当前阶段：三张视觉母版已完成，进入功能原型规划

## 1. 项目目标

做一套在门店长期稳定运行、能够灵活配置内容的屏幕播放系统：

- “当前正在播放”是屏幕的常驻主状态；
- CORD 品牌动效、豆子和门店活动只在合适的曲间低频插播；
- 演出信息附着在对应音乐人的当前播放页面上，不单独占用一次插播；
- 后台可以管理内容、有效期、优先级和插播频率，不需要每次改代码；
- 网络暂时中断或后台不可用时，门店屏幕仍能继续显示当前曲目或本地备用内容。

## 2. 已有基础与待补能力

| 模块 | 当前已有 | 还需要完成 |
|---|---|---|
| 曲目信息抓取 | Windows 已有 SMTC；macOS 已建立 Apple Music/Spotify 适配层 | 两个平台的实机兼容验收；Windows 播放位置/总时长；统一事件判断 |
| 数据桥接 | `webui/backend/bridge.py` 已提供统一 `state` | 扩充字段、拆分轻量轮询与封面更新、断线与异常状态 |
| 前端能力 | 当前 WebUI 可显示曲目、封面、进度和自定义内容 | 1920 × 1080 全屏播放端、三类场景切换、转场和防烧屏细节 |
| 视觉设计 | 三张 1920 × 1080 母版 | 品牌页、异常/无音乐状态、正式动效、不同长度文字适配 |
| 后台配置 | 现有配置偏窗口和网格布局 | 内容库、排期、频率规则、预览、发布、设备状态 |
| 播放调度 | 尚未实现 | 曲间插播状态机、频率上限、优先级、有效期和回退策略 |

## 3. 产品与播放规则

| 规则 | MVP 默认值 | 后台是否可调 | 说明 |
|---|---:|:---:|---|
| 当前播放占比 | 约 90%–95% | 是 | 不是三类内容平均轮播 |
| 单次品牌插播 | 10–15 秒 | 是 | 低频出现，正式素材完成前用母版验证 |
| 单次豆子/活动插播 | 12–18 秒 | 是 | 只显示已发布且在有效期内的内容 |
| 非音乐内容上限 | 每小时不超过 10% | 是 | 属于硬限制，避免屏幕变成广告牌 |
| 同类最小间隔 | 20–30 分钟 | 是 | 品牌、豆子、活动分别计时 |
| 连续插播 | 禁止 | 否 | 插播后至少完整显示一首或达到规定播放时长 |
| 插播触发点 | 自然曲目结束后 | 后续可扩展 | 暂停、拖动、手动切歌默认不触发 |
| 转场时长 | 0.6–0.9 秒 | 是 | 使用淡入淡出，不做强烈跳转动画 |
| 插播后返回 | 最新的当前曲目 | 否 | 插播期间后台继续更新曲目信息 |
| 无可播插播内容 | 继续显示当前播放 | 否 | 不为凑频率强行插播 |

## 4. 播放状态机

```text
启动 / 恢复
    ↓
读取最新曲目信息
    ↓
当前播放（常驻） ───────────────┐
    │                           │
    │ 自然结束且满足插播规则      │ 手动切歌 / 暂停 / 拖动
    ↓                           │
选择一条合格插播内容              │
    │                           │
    ├─ 无内容 → 读取最新曲目 ─────┤
    │                           │
    └─ 有内容 → 淡出 → 插播 → 淡出
                              ↓
                        读取最新曲目
                              ↓
                         当前播放
```

### 曲目结束判断

| 场景 | 判断信号 | 系统行为 |
|---|---|---|
| 自然结束 | 上一曲剩余时间已很短，随后曲目变化或播放停止 | 允许调度一次插播 |
| 手动切歌 | 曲目变化时上一曲仍有较多剩余时间 | 立即显示新曲，不插播 |
| 暂停 | 播放状态变为暂停，曲目未变化 | 保持当前画面，不启动倒计时插播 |
| 拖动进度 | 同一曲目位置发生明显跳变 | 重置结束预判 |
| 无时长信息 | 播放器未提供 position/duration | 降级为只识别换曲，默认不触发曲间插播 |
| 抓取暂时失败 | 短时间没有取得 SMTC 数据 | 保留上一条可信状态，并显示内部离线标记，不立即清空屏幕 |

## 5. 视觉设计清单

| 画面/组件 | 用途 | MVP 素材 | 正式版设计任务 | 优先级 |
|---|---|---|---|:---:|
| 当前正在播放 | 长期常驻主画面 | `母版_v01_01_正在播放` | 长标题、长音乐人名、无专辑封面、不同明暗封面的适配 | P0 |
| 正在播放＋演出提示 | 音乐人近期有相关演出时显示 | `母版_v01_02_正在播放_演出提示` | 演出数据字段、过期逻辑、信息密度和多场演出适配 | P0 |
| 豆子/活动 | 低频曲间插播 | `母版_v01_03_豆子活动` | 豆子与活动拆成可配置模板；图片、标题、正文、日期适配 | P0 |
| CORD 品牌动效 | 低频品牌插播 | 第一轮用静态占位 | 沿用“CORD 固定，扩展文字进入、组成含义、离开”的动效原则 | P1 |
| 无音乐/开店前 | 没有可用曲目时 | 暂用品牌静态页 | 品牌待机画面、提示是否对顾客可见 | P1 |
| 网络/服务异常 | 内部恢复状态 | 不单独对顾客报错 | 前台保持可观看画面；后台显示错误和最后更新时间 | P1 |
| 预览与安全区 | 后台预览电视实际效果 | 用三张母版 | 16:9 安全区、远距离字号、电视色彩和裁切检查 | P0 |

## 6. 后台配置范围

### 6.1 内容库字段

| 字段 | 示例 | 用途 |
|---|---|---|
| 内容名称 | 九月埃塞俄比亚新豆 | 后台识别 |
| 内容类型 | 品牌 / 豆子 / 活动 | 参与对应频率规则 |
| 使用模板 | 豆子母版 A | 决定前端版式 |
| 主图/视频 | 本地上传素材 | 正式播放资源 |
| 标题、正文、辅助信息 | 产区、处理法、活动时间 | 填入模板 |
| 单次时长 | 15 秒 | 控制插播长度 |
| 生效与失效时间 | 9 月 1 日至 9 月 15 日 | 防止过期内容继续显示 |
| 每日时段 | 10:00–18:00 | 按营业时段投放 |
| 优先级 | 普通 / 高 | 多条合格内容间排序 |
| 最小间隔 | 30 分钟 | 控制重复出现 |
| 每日上限 | 8 次 | 控制总曝光量 |
| 发布状态 | 草稿 / 已发布 / 已停用 | 人工审核后上线 |

### 6.2 第一版后台页面

| 页面 | 必须显示/操作 | MVP |
|---|---|:---:|
| 运行总览 | 设备在线状态、当前曲目、当前画面、最后更新时间、下一条候选插播 | 是 |
| 内容库 | 新建、编辑、复制、停用、预览品牌/豆子/活动内容 | 是 |
| 播放规则 | 非音乐占比、各类型最小间隔、每日上限、允许时段 | 是 |
| 30 分钟模拟预览 | 用模拟歌曲快速演算调度结果，不必真实等待 | 是 |
| 发布记录 | 谁在何时发布了什么配置、当前生效版本 | 第二阶段 |
| 设备管理 | 多门店、多屏幕、分组下发 | 第二阶段 |

## 7. 技术模块划分

| 模块 | 建议职责 | 第一版实现方向 |
|---|---|---|
| Track Source | 从 macOS Apple Music/Spotify 或 Windows SMTC 获取曲目、状态、时间线 | 共用接口＋两个平台适配器；Windows 扩展现有 `get_music_powershell.py` |
| State Bridge | 统一字段、缓存、错误恢复、只在换曲时更新封面 | 扩展现有 `webui/backend/bridge.py` |
| Scheduler | 判断自然结束、筛选插播、执行频控 | 新建独立、可测试的状态机 |
| Display Frontend | 1920 × 1080 全屏显示、模板渲染、转场 | Web 前端；与后台配置解耦 |
| Admin Backend | 内容 CRUD、排期、规则、发布 | 本机服务，提供 API |
| Storage | 配置、内容元数据、播放与错误日志 | MVP 使用 SQLite＋本地素材目录 |
| Simulator | 加速模拟曲目结束、切歌、暂停和插播 | 复用现有 `mock.js`，增加时间轴控制 |
| Device Runtime | 开机启动、崩溃恢复、全屏、版本更新 | macOS 与 Windows 分别提供启动和部署适配 |

## 8. 整体开发路线图

工时为单人开发和设计的粗估，不含等待真实门店运行观察的时间。

| 阶段 | 工作内容 | 主要交付物 | 验收条件 | 预计工时 | 状态 |
|---|---|---|---|---:|:---:|
| 0. 范围冻结 | 确认 MVP、默认播放规则、数据字段和不做事项 | 本规划表 v0.1、数据协议草案 | 产品规则无关键歧义 | 0.5–1 天 | 当前 |
| 1. 曲目信息补全 | macOS/Windows 数据源适配；增加 position/duration；优化轮询；区分换曲、暂停、拖动和自然结束 | 统一 track state、双平台适配器、事件日志 | 两个平台分别完成真实播放器测试；事件可追踪；无时长时安全降级 | 3–5 天 | 已开始 |
| 2. 功能模拟器 | 模拟短曲、自然结束、手动切歌、暂停、抓取失败；可加速时间 | 浏览器调试页、固定测试场景 | 不连接播放器也能复现全部核心状态 | 1–2 天 | 部分完成：自然结束按钮 |
| 3. 三母版前端 MVP | 把三张母版转为数据驱动页面；实现淡入淡出和最新曲目回切 | 1920 × 1080 播放端原型 | 三类画面可由 mock 数据驱动；无溢出、无黑场 | 3–5 天 | 已完成 MVP（2026-09-02） |
| 4. 调度器 MVP | 实现曲间插播、有效期、最小间隔、每日上限、禁止连续插播 | 可测试的调度状态机 | 模拟 1 小时结果符合规则；手动切歌不误触发 | 3–4 天 | 核心路径完成；待每日上限和长模拟 |
| 5. 后台 MVP | 内容库、规则配置、图片上传、预览、发布、运行总览 | 本机后台＋SQLite | 非开发人员可完成新增、预览、发布、停用 | 4–6 天 | 部分完成：演出表＋豆子/活动库＋插播规则 |
| 6. 正式视觉与动效 | 优化三母版；完成品牌动效；补无音乐状态和文字边界 | 正式模板、动效资源、设计规范 | 电视实机远距离可读；转场克制；素材格式稳定 | 3–5 天 | 可与 4–5 并行 |
| 7. 门店设备部署 | macOS/Windows 全屏启动、自动启动、异常重启、本地缓存、日志导出 | 两个平台的启动入口、安装包与部署说明 | 两个平台断网和应用重启后可自行恢复；不露出桌面 | 3–5 天 | 待开始 |
| 8. 试运行与修正 | 门店连续运行；记录误判、视觉疲劳、内容频率和恢复问题 | 试运行记录、问题修复、v1.0 配置 | 连续 5–7 个营业日无阻断问题 | 5–7 个营业日 | 待开始 |

预计：开发与设计约 15.5–24 个工作日，之后进行 5–7 个营业日实店试运行。阶段 4/5 与阶段 6 可并行，因此实际日历时间可短于工时总和。

## 9. 第一轮功能验证范围

先证明播放逻辑成立，不等待正式后台和最终动效。

| 验证项 | 使用素材/方法 | 通过标准 |
|---|---|---|
| 当前曲目实时更新 | 母版 01＋真实抓取/模拟数据 | 换曲后在目标时间内更新，封面与文字一致 |
| 演出提示条件显示 | 母版 02＋手动 mock 字段 | 有演出才出现；无演出时版式自动恢复 |
| 豆子/活动曲间插播 | 母版 03＋短曲模拟 | 只在自然结束后出现；结束后回到最新曲目 |
| 手动切歌保护 | 在歌曲中段触发 next | 不插播，直接进入新曲画面 |
| 插播期间换曲 | 插播时触发下一曲 | 插播不中断；结束后展示最新曲目，不返回旧曲 |
| 频率限制 | 加速模拟 60 分钟 | 无连续插播；非音乐时长不超过设定上限 |
| 异常恢复 | 模拟抓取超时、短暂断网、前端刷新 | 不出现长时间黑屏，恢复后自动追上最新状态 |

## 10. MVP 明确不做

- 第一版不做多门店、多设备分组管理；
- 第一版不自动抓取演出信息，先由后台人工录入并设置有效期；
- 第一版不做复杂账号权限，只保留本机管理员入口；
- 第一版不把现有 WebUI 的自由网格编辑器直接作为最终后台；
- 第一版不承诺所有播放器都能提供精确时长；缺失时采用安全降级；
- 第一版不让生成式内容自动发布，所有对外信息保留人工确认。

## 11. 风险与应对

| 风险 | 影响 | 应对方式 |
|---|---|---|
| 某些音乐播放器不提供准确时间线 | 无法可靠判断自然结束 | 建立播放器兼容清单；缺失时禁用自动曲间插播 |
| PowerShell 高频轮询负担过大 | 卡顿或更新延迟 | 轻量时间线与封面抓取分开；封面只在换曲时更新 |
| 文字长度、封面明暗差异很大 | 母版在真实数据下失效 | 建立极端测试数据；限制行数、字号阶梯和颜色兜底 |
| 插播太频繁 | 顾客注意力被打断 | 小时占比硬上限、同类间隔、禁止连续插播 |
| 后台错误配置或内容过期 | 显示错误信息 | 草稿/发布机制、有效期、预览和一键停用 |
| 门店网络或服务异常 | 黑屏或停在错误页面 | 播放端本地化、缓存最后状态、自动重连和进程守护 |
| 电视与电脑色彩/裁切不同 | 设计稿与现场观感不一致 | 用真实 32LS03C 做字号、色彩、安全区和远距离验收 |

## 12. 建议的下一步

第一开发迭代只做以下四项：

1. 定义扩展后的 `track state` 数据协议；
2. 为现有抓取系统补充 position/duration 和事件判断；
3. 建立可加速的播放模拟器；
4. 将三张母版接成一个可运行的全屏原型。

完成后先进行一次桌面功能评审，再决定后台 MVP 的具体交互和正式品牌动效，不在尚未验证调度逻辑前投入全部视觉制作。

## 13. 确认后的运行形态

第一版可以使用一台 macOS 或 Windows 电脑完成抓取、配置和显示，不依赖远程服务器：

```text
本地服务（隐藏运行）
├─ /display  → 三星屏幕上的全屏 HTML
├─ /admin    → 单独窗口中的配置 HTML
├─ /api      → 曲目、内容、规则和运行状态
└─ SQLite＋本地素材目录
```

- `display` 和 `admin` 是两个独立 HTML 入口，可以在两个窗口中同时打开；
- 配置窗口关闭后，显示和调度继续运行；
- 点击发布后，显示端实时取得新配置，不需要手动刷新；
- 电脑开机时自动启动本地服务和全屏显示窗口；
- 所有核心功能在本机可用，互联网只作为未来扩展能力。

## 14. 分轮开发与评审节点

每一轮都必须形成一个能运行、能看到、能单独验收的版本；上一轮通过后再扩大范围。

| 轮次 | 本轮目标 | 具体开发内容 | 本轮可演示结果 | 通过标准 | 预计工时 |
|---|---|---|---|---|---:|
| 第 1 轮：本机双页面骨架 | 验证“一台电脑、两个 HTML 窗口” | 建立本地服务；`/display`、`/admin`、`/api/state`、健康检查；定义 track/display/config 数据协议；使用 mock 曲目；建立基础测试 | 同时打开显示页和配置页；配置页改变模拟曲目后，显示页实时更新 | 两页可独立刷新；服务重启后恢复；无网络也可运行；项目启动方式明确 | 1–2 天 |
| 第 2 轮：真实曲目接入 | 把 macOS 与 Windows 播放器稳定接入同一骨架 | 共用 TrackSource 接口；macOS 支持 Apple Music/Spotify；Windows 复用 SMTC；增加 position/duration；拆分高频时间线与低频封面读取；事件分类；保留 mock/真实切换 | 同一显示页分别实时显示两种系统上的标题、音乐人、专辑、封面、进度和状态 | 两个平台分别实机验收；自然结束、手动切歌、暂停、拖动有可追踪事件；数据缺失时不黑屏 | 3–5 天 |
| 第 3 轮：三母版显示 MVP（已完成） | 把静态母版变成真实 HTML 页面 | 已实现正在播放、演出提示、豆子/活动三个模板；左侧视觉支持专辑封面、自定义图片、循环视频；长文字两行适配；封面兜底；750 ms 交叉淡入淡出；1920 × 1080 安全区 | 已用 mock 数据、用户自定义图片完整演示三张母版和转场 | 18 项自动化测试通过；三张母版完成 1920 × 1080 浏览器检查；自动插播仍留到第 4 轮 | 已完成（2026-09-02） |
| 第 4 轮：曲间调度（核心路径完成） | 验证核心播放逻辑 | 已完成音乐人自动命中、自然结束识别、豆子/活动有效候选轮换、最小间隔、小时占比上限、禁止连续插播、插播期间更新曲目及定时回切；Windows SMTC 已补时间线读取；待完成品牌候选、每日次数上限和加速长模拟 | 曲目自然结束后自动显示一条有效内容，到时后回到最新曲目；暂停和中段手动切歌不触发 | 36 项测试及浏览器 5 秒插播/回切演练通过；Windows 时间线待实机验收 | 3–4 天 |
| 第 5 轮：后台配置 MVP（部分完成） | 让门店人员不改代码即可更新内容 | 已完成 SQLite 音乐人演出表与豆子/活动内容库；两者均支持多条记录的新增、编辑、启停、删除和单条预览；豆子/活动另有生效期、优先级、展示秒数及可选图片；后台可配置自动插播开关、最小间隔和小时占比，并可一键模拟自然结束；待完成品牌内容库、发布流程和 30 分钟预演 | 可在 `admin` 维护多条演出和豆子/活动内容，并完成自动插播规则配置与即时验证 | 演出表、内容库、纯图片和曲终自动插播流程已通过；品牌内容及发布机制仍待接入 | 4–6 天 |
| 第 6 轮：正式设计与动效 | 将功能原型提升为门店正式视觉 | 优化三套模板；完成 CORD 品牌动效；补充无音乐/开店前状态；远距离字号；色彩、裁切和素材规范；实机调整 | 三星屏幕上演示完整正式播放循环 | 现场观看距离可读；动效克制；画面符合 CORD 视觉；不同封面下保持稳定 | 3–5 天 |
| 第 7 轮：部署与试运行 | 形成可长期使用的双平台版本 | macOS/Windows 开机自启；显示器指定；全屏/Kiosk；崩溃重启；本地缓存；日志导出；备份恢复；安装与操作说明 | 两个平台重启后都能自动进入营业状态，配置端仍可单独打开 | macOS 与 Windows 各完成部署验收；门店目标电脑连续 5–7 个营业日无阻断问题 | 3–5 天开发＋5–7 个营业日观察 |

### 每轮固定交付格式

| 交付项 | 内容 |
|---|---|
| 可运行版本 | 该轮功能可以在目标电脑或开发机上实际打开 |
| 演示入口 | 明确启动方式、`display` 地址和 `admin` 地址 |
| 验收清单 | 逐项标记通过、未通过和限制条件 |
| 截图/录像 | 保留关键状态和异常恢复的视觉记录 |
| 变更记录 | 说明新增功能、已知问题和下一轮依赖 |
| 回退点 | 每轮保留一个稳定版本，下一轮失败不影响已验收功能 |

### 第 1 轮任务拆分

| 编号 | 任务 | 输出 | 顺序 |
|---|---|---|:---:|
| R1-01 | 建立独立项目结构和一条启动命令 | 项目骨架、README、启动脚本 | 1 |
| R1-02 | 建立本地 HTTP 服务 | `/health`、`/api/state` | 2 |
| R1-03 | 建立统一数据协议 | `track`、`display`、`config` 示例和校验 | 2 |
| R1-04 | 建立 `display` HTML | 全屏画布、当前状态和连接状态占位 | 3 |
| R1-05 | 建立 `admin` HTML | mock 曲目输入、播放/暂停/换曲按钮、连接状态 | 3 |
| R1-06 | 建立实时同步 | 配置页操作后显示页无需刷新即可变化 | 4 |
| R1-07 | 建立最小测试 | API、状态更新、页面入口和无网络运行检查 | 5 |
| R1-08 | 完成本轮验收记录 | 启动说明、截图、通过项和下一轮接口 | 6 |

## 15. 双平台维护原则

- 产品核心只保留一套：HTML、API、状态协议、调度器、内容模型和 SQLite 全部共用；
- 平台差异限制在 `app/sources/macos.py`、`app/sources/windows.py` 和各自启动/部署脚本；
- macOS 以 MediaRemote 读取控制中心信息，覆盖已验证的 Apple Music、QQ音乐及待验证的网易云，并保留 Apple Music/Spotify 的 AppleScript 降级；Windows 第一适配范围为发布 SMTC 信息的播放器；
- 每一轮的自动测试在两个系统都要通过，涉及播放器和启动行为的项目还需要分别实机验收；
- 没有对应系统的实测证据时，状态写“代码完成，待平台验收”，不写成已经通过；
- 不建立两套长期分叉的 macOS/Windows 前端，避免视觉和业务规则逐渐不一致。
