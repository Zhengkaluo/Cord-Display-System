# Third-party notices

## nowplaying-cli

The optional macOS system-wide Now Playing adapter executes `nowplaying-cli` as a separate command-line tool.

- Project: <https://github.com/kirtan-shah/nowplaying-cli>
- Pinned version: v2.1.0
- License: GPL-3.0-or-later
- Purpose: read macOS MediaRemote metadata exposed in Control Center

The binary is installed locally under `.tools/` and is not tracked with the FlowSystem source. Run `scripts/setup-macos-mediaremote.sh` to reproduce the local installation from the pinned upstream source commit.
